import { ethers } from "ethers";

export default async function sendToken() {
  try {
   
    const receipt = "hello";

    return receipt;
  } catch (error) {
    console.error(error);
    return null;
  }
};

